# Procfile

web: gunicorn --bind 0.0.0.0:$PORT app:app
# worker: python worker.py   <-- NOTE: This line would be used if running locally or using a different deployment platform. On Railway, you create a *separate* service for the worker.
