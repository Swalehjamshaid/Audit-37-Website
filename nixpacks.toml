# nixpacks.toml
# Install all necessary dependencies for WeasyPrint, covering Pango, Cairo, 
# and the GObject/GLib dependencies required by the crash logs.
[phases.setup]
nixPkgs = [
    "pkg-config", 
    "cairo", 
    "pango", 
    "gobject-introspection", 
    "glib"
]
