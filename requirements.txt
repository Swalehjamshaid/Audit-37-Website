# Core web server & framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=23.0.0

# Data validation & settings
pydantic>=2.8.0
pydantic-settings>=2.4.0

# HTTP & forms
httpx>=0.27.0
python-multipart>=1.0.0

# Utilities (most projects need these)
python-dotenv>=1.0.1
requests>=2.32.0

# Security (if you use auth)
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Database (remove if you don't use)
# sqlalchemy>=2.0.0
# psycopg2-binary>=2.9.9

# ──────────────────────────────
# THIS LINE IS THE FINAL FIX
# It installs your own code (including the 'webaudit' folder) as a real package
-e .
# ──────────────────────────────
